[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "types-requests>=2.33.0.20260408",
]
